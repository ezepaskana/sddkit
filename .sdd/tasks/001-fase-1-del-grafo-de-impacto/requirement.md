# Requisito original — tarea 001

> Capturado verbatim el 2026-06-12. **No editar este archivo**: el refinamiento va en spec.md.

Fase 1 del grafo de impacto cross-sistema (ver REQUISITO-grafo-impacto.md, secciones 1-3 Fase 1, 5, 6 F1 y 7): Detector de consumos salientes — espejo del índice de endpoints expuestos. Detectar llamadas HTTP salientes del código (JS/TS: fetch(, axios.<verbo>(, axios( con config, clientes http comunes; Java: RestTemplate, WebClient, HttpClient, OkHttp, cubriendo el estilo del repo backend-service con Javalin) con el mismo patrón determinístico que extractEndpoints en src/lib/patterns.js. Capturar método (si es inferible), URL o path, y archivo; URLs desde env vars se registran como destino simbólico env:NOMBRE_VAR (la resolución llega en Fase 3). sdd scan debe persistir esto en patterns.json -> capabilities.consumptions y sdd find debe buscar también ahí. Registrar también una sección nueva 'Dependencias salientes' en el C4 nivel 2 (.sdd/c4/containers.md). Las 'Decisiones ya tomadas' de la sección 5 del requisito (archivos como fuente de verdad, storage enchufable SQLite/MySQL, publish desde CI sobre main, sdd impact como advertencia no gate, Terraform via terraform show -json solo metadatos, IAM sin uso = arista potencial, join keys por tipo) se registran como ADRs en .sdd/decisions/ durante esta fase y se citan desde la spec, sin re-discutirlas. Piloto de validación: repos backend-service y frontend-app en D:\Desarrollo. Métrica F1 (sección 6): cantidad de consumos salientes detectados en esos repos vs los reales (revisión manual del dev), baseline 0 (hoy no se detecta ninguno).
